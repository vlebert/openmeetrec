/**
 * Offscreen document — tient la capture et l'enregistrement pendant toute la session.
 *
 * Squelette : mix Web Audio, ré-injection du son de l'onglet (F-CAP-06) et
 * ChunkScheduler (architecture §4.2) arrivent dans l'incrément suivant.
 */

// TODO(F-CAP-02/04) : getUserMedia micro + onglet, mix mono, monitoring.
export {};
